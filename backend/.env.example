# Wispr Flow API key (from https://platform.wisprflow.ai — API Keys).
# Your key looks like: fl-xxxxxxxx...
# Use either variable name; backend checks both.
WISPR_FLOW_API_KEY=
# WHISPER_API_KEY=   # optional; same key works if you already have it in root .env
